using UnityEngine;

public class ControladorPlayer : MonoBehaviour
{
    [Min(0)]
    public int contadorCoins = 0;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
